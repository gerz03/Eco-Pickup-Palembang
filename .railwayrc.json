{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "dockerfile",
    "buildpacks": []
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "php -S 0.0.0.0:${PORT:-8080} -t /app"
  }
}
